---
title: Listbox
description: An accessible listbox component with single and multiple selection, keyboard navigation, search, and customizable icons.
preview: true
links:
  api: /docs/components/listbox#api-reference
---

<ComponentTabs name="listbox-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add "https://maksud.dev/r/listbox"
```

### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     lucide-react
     ```
  </Step>
  <Step>
    Make sure you have the following components installed:

    - [Scroll Area](/docs/components/scroll-area)
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="listbox" />
  </Step>
</Steps>

## Layout

Import the component and use it in your application.

```tsx
import { Listbox } from "@/components/ui/listbox";

const options = [
  { label: "Option 1", value: "1" },
  { label: "Option 2", value: "2" },
  { label: "Option 3", value: "3" },
];

export default function Example() {
  const [value, setValue] = React.useState("");

  return (
    <Listbox
      options={options}
      value={value}
      onChange={setValue}
    />
  );
}
```

## Examples

### Single Selection

<ComponentTabs name="listbox-single-demo" />

Select a single option from the list.

### Multiple Selection

<ComponentTabs name="listbox-multiple-demo" />

Select multiple options using the `multiple` prop.

### With Search

<ComponentTabs name="listbox-searchable-demo" />

Enable search functionality with the `searchable` prop to filter options.

### With Descriptions

<ComponentTabs name="listbox-with-descriptions-demo" />

Display additional information for each option using the `description` field.

### Custom Icon

<ComponentTabs name="listbox-custom-icon-demo" />

Customize the selection indicator icon with the `icon` prop.

## API Reference

### Listbox

<PropsTable
  data={[
    {
      title: "options",
      type: "ListboxOption[]",
      default: "required",
      description: "Array of options to display.",
    },
    {
      title: "value",
      type: "string | string[]",
      default: "undefined",
      description: "Selected value(s). Use string for single selection, string[] for multiple.",
    },
    {
      title: "onChange",
      type: "(value: string | string[]) => void",
      default: "required",
      description: "Callback when selection changes.",
    },
    {
      title: "multiple",
      type: "boolean",
      default: "false",
      description: "Enable multiple selection mode.",
    },
    {
      title: "searchable",
      type: "boolean",
      default: "false",
      description: "Enable search input to filter options.",
    },
    {
      title: "searchPlaceholder",
      type: "string",
      default: "'Search...'",
      description: "Placeholder text for the search input.",
    },
    {
      title: "emptyMessage",
      type: "string",
      default: "'No options available'",
      description: "Message displayed when no options match the search or list is empty.",
    },
    {
      title: "icon",
      type: "React.ComponentType<{ className?: string }>",
      default: "Check (from lucide-react)",
      description: "Icon component displayed next to selected items.",
    },
    {
      title: "className",
      type: "string",
      default: "undefined",
      description: "Additional CSS classes for the container.",
    },
  ]}
/>

### ListboxOption

<PropsTable
  data={[
    {
      title: "label",
      type: "string",
      default: "required",
      description: "Display text for the option.",
    },
    {
      title: "value",
      type: "string",
      default: "required",
      description: "Unique identifier for the option.",
    },
    {
      title: "disabled",
      type: "boolean",
      default: "false",
      description: "Disable the option from being selected.",
    },
    {
      title: "description",
      type: "string",
      default: "undefined",
      description: "Optional description text shown below the label.",
    },
  ]}
/>

## Keyboard Navigation

The Listbox component supports full keyboard navigation:

- `↑` `↓` - Navigate through options
- `Enter` / `Space` - Select focused option
- `Home` / `End` - Jump to first/last option
- Focus is automatically managed and visible

## Accessibility

- Uses proper ARIA roles (`listbox`, `option`) for screen reader support
- Full keyboard navigation with arrow keys, Enter, Space, Home, and End
- Proper focus management with visible indicators
- Selected and disabled states are announced to assistive technologies
- Search input is properly labeled for accessibility
