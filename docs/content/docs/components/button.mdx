---
title: Button
description: A versatile button component with multiple variants, sizes, and states for user interactions.
preview: true
links:
  api: /docs/components/button#api-reference
---

<ComponentTabs name="button-demo" />

## Installation

### CLI

```package-install
npx shadcn@latest add "https://maksud.dev/r/button"
```

### Manual

<Steps>
  <Step>
     Install the following dependencies:

     ```package-install
     @radix-ui/react-slot class-variance-authority
     ```
  </Step>
  <Step>
    Copy and paste the following code into your project.

    <ComponentSource name="button" />
  </Step>
</Steps>

## Layout

Import the component and use it in your application.

```tsx
import { Button } from "@/components/ui/button";

export default function Example() {
  return (
    <Button>Click me</Button>
  );
}
```

## Examples

### Variants

<ComponentTabs name="button-variants-demo" />

The Button component supports eight different variants: `default`, `destructive`, `warning`, `outline`, `secondary`, `ghost`, `link`, and `success`.

### Sizes

<ComponentTabs name="button-sizes-demo" />

Choose from five different sizes: `default`, `sm`, `lg`, `icon`, and `pill`.

### With Icons

<ComponentTabs name="button-with-icons-demo" />

Buttons can include icons to provide additional visual context and improve usability.

### Loading State

<ComponentTabs name="button-loading-demo" />

Use the `loading` prop to show a loading state and disable user interaction.


## API Reference

<PropsTable
  data={[
    {
      title: "variant",
      type: "'default' | 'destructive' | 'warning' | 'outline' | 'secondary' | 'ghost' | 'link' | 'success'",
      default: "'default'",
      description: "The visual style variant of the button.",
    },
    {
      title: "size",
      type: "'default' | 'sm' | 'lg' | 'icon' | 'pill'",
      default: "'default'",
      description: "The size of the button.",
    },
    {
      title: "asChild",
      type: "boolean",
      default: "false",
      description: "When true, renders as a child component (e.g., Link) instead of a button element.",
    },
    {
      title: "loading",
      type: "boolean",
      default: "false",
      description: "When true, shows loading state and disables the button.",
    },
    {
      title: "disabled",
      type: "boolean",
      default: "false",
      description: "When true, disables the button and prevents user interaction.",
    },
  ]}
/>

## Accessibility

- The button uses semantic `<button>` element by default
- Supports all standard button HTML attributes including `disabled`
- Proper focus management with visible focus indicators
- Screen reader accessible with appropriate ARIA attributes
- Loading and disabled states are properly communicated to assistive technologies
